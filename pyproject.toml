[project]
name = "demolangchain"
version = "0.1.0"
description = "Proyecto con agentes de Langchain"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "langchain>=1.2.7",
    "langchain-community>=0.4.1",
    "langgraph>=1.0.7",
]
